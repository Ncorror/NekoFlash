# Reference provenance

The immutable evidence base is the separately supplied bundle containing exact Legacy, A2 and current snapshot archives. This production tree records their hashes but does not duplicate the large archives.

Brand files that must be directly available to the rewrite are copied from Legacy with hash gates:

- production Welcome JPEG in `app/src/main/res/drawable-nodpi/bg_welcome.jpg`;
- launcher reference in `reference/brand/nekoflash-launcher-reference.png`.
